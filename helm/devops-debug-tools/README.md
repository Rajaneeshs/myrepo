# Default values for devops-debug-tools Helm chart
# To use a custom service account, set serviceAccount.create to false and provide a name:
# helm install my-debug-tools ./devops-debug-tools --set serviceAccount.create=false --set serviceAccount.name=my-custom-sa
